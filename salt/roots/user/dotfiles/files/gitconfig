[color]
	ui = true
[user]
	name = Mark Lee
{%- set git_email = salt['pillar.get']('git:email', false) %}
{%- if git_email %}
	email = {{ git_email }}
{%- endif %}
[alias]
	co = checkout
[push]
	default = simple
{%- if salt['pillar.get']('github:version') %}
[hub]
	protocol = ssh
{%- endif %}
