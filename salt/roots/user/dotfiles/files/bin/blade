#!/bin/bash
#
# Wrapper for knife when the user is running RVM.
#

[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"  # This loads RVM into a shell session.

if [[ -z "$KNIFE_ENV" ]]; then
    KNIFE_ENV=system
fi

rvm use "$KNIFE_ENV" 1>&2

knife "$@"
