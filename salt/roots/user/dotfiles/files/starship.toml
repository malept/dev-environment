# This is autogenerated by saltstack dev-environment.
{%- if salt['pillar.get']('nushell:enabled') %}
[custom.shell]
command = "case $SHELL in */bash) echo 🔨;; */nu) echo 🥜;; esac"
symbol = "🐚"
shell = ["bash"]
when = 'test -n "$SHELL"'
{%- endif %}
{%- if salt['pillar.get']('docker:k8s:enabled') %}
[kubernetes]
disabled = false
style = "dimmed blue"

[[kubernetes.contexts]]
context_pattern = "dev-environment"
context_alias = "dev"

[[kubernetes.contexts]]
context_pattern = ".*"
user_pattern = "cluster-user"
style = "red bold"
symbol = "🚨 "
{%- endif %}
